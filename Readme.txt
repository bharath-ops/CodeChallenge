1. Application was built on ASP.NET MVC. Open the solution in VS2107, build and run it.
2. I have modified the interface method "CalculateOhmValue" return type to double from int as ohms value can be in decimal.
3. Implimented unit test using MSTest.
